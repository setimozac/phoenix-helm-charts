apiVersion: v1
kind: Pod
metadata:
  namespace: {{ .Release.namespace }}
  name: "backend-test-connection"
  labels:
    app.kubernetes.io/name: phoenix-backend-connection-test
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['phoenix-backend:80/']
  restartPolicy: Never
