apiVersion: apps/v1
kind: Deployment
metadata:
  name: phoenix-envmanager-backend
  namespace: {{ .Release.namespace }}
  labels:
    operator-controller: phoenix-envmanager-backend
    app.kubernetes.io/name: phoenix-backend
spec:
  {{- if not .Values.phoenix-backend.autoscaling.enabled }}
    replicas: {{ .Values.phoenix-backend.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      operator-controller: phoenix-envmanager-backend
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: phoenix-backend
      labels:
        operator-controller: phoenix-envmanager-backend
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
      - command:
        - /app
        - -dsn=host=phoenix-backend-db.operators.svc.cluster.local port=5432 user=postgres password=postgres dbname=env_manager timezone=UTC connect_timeout=5
        name: {{ .Values.phoenix-backend.containerName }}
        image: "{{ .Values.phoenix-backend.image.repository }}:{{ .Values.phoenix-backend.image.tag }}"
        ports:
          - containerPort: {{ .Values.phoenix-backend.container.port }}
        imagePullPolicy: {{ .Values.phoenix-backend.image.pullPolicy }}
        resources:
          limits:
            cpu: 500m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 64Mi
      serviceAccountName: {{ .Values.phoenix-backend.serviceAccount.name }}
      terminationGracePeriodSeconds: 10